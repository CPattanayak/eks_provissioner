apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: EKS-course-cluster4
  region: ap-south-1

nodeGroups:
  - name: ng-1
    instanceType: t3.small
    desiredCapacity: 1
    privateNetworking: true
    ssh: # use existing EC2 key
      allow: true
      publicKeyName: eks_cluster